{
  "name": "deha-docs",
  "private": true,
  "scripts": {
    "dev": "mintlify dev",
    "build": "mintlify export && mkdir -p out && cd out && unzip -o ../export.zip && rm ../export.zip"
  },
  "dependencies": {
    "@vercel/edge": "^1.3.1",
    "mintlify": "^4.0.0"
  }
}
